{
  "name": "vitepress-open-md",
  "publisher": "chgeo",
  "displayName": "Open Markdown Document in VitePress",
  "description": "Open the currently active markdown file in the running VitePress dev server.",
  "version": "0.0.1",
  "license": "MIT",
  "keywords": [
    "vitepress",
    "markdown",
    "browser",
    "documentation"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/chgeo/vitepress-open-md.git"
  },
  "homepage": "https://github.com/chgeo/vitepress-open-md#readme",
  "bugs": {
    "url": "https://github.com/chgeo/vitepress-open-md/issues"
  },
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "vitepressOpenCurrent.openCurrent",
        "title": "Open VitePress Markdown in Browser"
      }
    ],
    "configuration": {
      "title": "VitePress Open Markdown",
      "properties": {
        "vitepressOpenCurrent.baseUrl": {
          "type": "string",
          "default": "http://localhost:5173",
          "description": "Base URL of the running VitePress dev server."
        },
        "vitepressOpenCurrent.rootFolder": {
          "type": "string",
          "default": "docs",
          "description": "Workspace folder that contains the VitePress root."
        },
        "vitepressOpenCurrent.browserApp": {
          "type": "string",
          "default": "Google Chrome",
          "description": "macOS external browser app for tab reuse. Use Google Chrome, Safari, or another Chrome-family app name."
        },
        "vitepressOpenCurrent.anchor": {
          "type": "string",
          "default": "",
          "description": "Optional anchor to append"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p .",
    "watch": "tsc -watch -p .",
    "package": "npx vsce package"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/vscode": "^1.90.0",
    "typescript": "^5.5.0"
  }
}
